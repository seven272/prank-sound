services:
  prank-mongo:
    image: mongo:8.0
    container_name: prank_mongo_container
    networks:
      - nginx-rp-net
    restart: always
    volumes:
      - mongo_database:/data/db
  prank-server:
    build:
      dockerfile: Dockerfile
      context: ./server
    container_name: prank_server_container
    hostname: prank_server_host
    networks:
      - nginx-rp-net
    ports:
      - '${SERVER_OUTSIDE_PORT}:${SERVER_DOCKER_PORT}'
    environment:
      MONGO_URI: ${MONGO_URI}
    volumes:
      - uploads_data:/app/src/uploads
    restart: always
    depends_on:
      - prank-mongo
  prank-client:
    build:
      dockerfile: Dockerfile
      context: ./client
    container_name: prank_client_container
    hostname: prank_client_host
    networks:
      - nginx-rp-net
    ports:
      - '${CLIENT_OUTSIDE_PORT}:${CLIENT_DOCKER_PORT}'

    restart: always
    depends_on:
      - prank-server
  prank-mongo-express:
    image: mongo-express:1.0
    container_name: prank_mongo_express_container
    networks:
      - nginx-rp-net
    ports:
      - '${ME_OUTSIDE_PORT}:${ME_DOCKER_PORT}'
    environment:
      ME_CONFIG_MONGODB_URL: ${ME_CONFIG_MONGODB_URL}
      ME_CONFIG_BASICAUTH_USERNAME: ${ME_CONFIG_BASICAUTH_USERNAME}
      ME_CONFIG_BASICAUTH_PASSWORD: ${ME_CONFIG_BASICAUTH_PASSWORD}

    restart: always

volumes:
  mongo_database:
  uploads_data:

networks:
  nginx-rp-net:
    external: true # указывает на использование существующей сети, а не создание новой
